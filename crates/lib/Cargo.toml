[package]
name = "mud-lib"
version = "0.2.0"
edition = "2021"

[dependencies]
mud-client = { version = "0.2.0", path = "../client" }
mud-opts = { version = "0.2.0", path = "../opts" }
mud-packet = { version = "0.2.0", path = "../packet" }
